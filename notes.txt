================================= 1. Basic ==============================================
// Basic
# 1. नया folder बनाओ और उसमें जाओ
mkdir myproject
cd myproject

# 2. Git repo initialize करो
git init

# 3. एक नई file बनाओ (example)
echo "Hello Git" > hello.txt

# 4. Repo की स्थिति check करो
git status

# 5. File को staging area में डालो
git add hello.txt
# (या सब फाइलें add करनी हों तो: git add .)

# 6. First commit बनाओ
git commit -m "First commit: added hello.txt"

# 7. GitHub पर नई repo बनाओ (manual step GitHub website पर)

# 8. Remote repo से connect करो
git remote add origin https://github.com/username/myproject.git

# 9. Main branch set करो
git branch -M main

# 10. Code GitHub पर push करो
git push -u origin main



================================= 2. Branch ==============================================
# 1. Branch list देखना
git branch

# 2. नई branch बनाना
git branch feature1

# 3. उस branch पर switch करना
git checkout feature1

# (Shortcut: branch बनाना + switch करना एक साथ)
git checkout -b feature2

# 4. फाइल add करके commit करना (branch में काम करो)
echo "This is feature work" > feature.txt
git add feature.txt
git commit -m "Added feature.txt in feature branch"

# 5. Branch को GitHub पर push करना
git push -u origin feature1

# 6. वापस main branch पर आना
git checkout main

# 7. feature1 branch को main में merge करना
git merge feature1

# 7.1 Merge result को GitHub पर push करना (important)
git push origin main

# 8. Branch delete करना (local)
git branch -d feature1

# 9. Branch delete करना (remote/GitHub)
git push origin --delete feature1

🔹 3. अगर दोनों जगह से हटाना है
तो पहले local delete करो और फिर remote delete:
git branch -d branch_name
git push origin --delete branch_name

====================================== Veri IMP ===============================
==================== (Untracked Files) VS (Modified Tracked Files) ============

## 🔹 Case 1: **Untracked Files** (नई फाइलें, `git add` या `commit` नहीं किया)

* Git को इनकी कोई history नहीं पता।
* ये सिर्फ आपके working directory में पड़ी हैं।
* इसलिए आप आसानी से branch बदल सकते हो (`git checkout branch_name`)।
* Branch change करने से untracked files वहीं रह जाती हैं (क्योंकि वो किसी branch से जुड़ी ही नहीं हैं)।

👉 Example:

echo "hello" > new.txt   # untracked file
git checkout master      # allowed

## 🔹 Case 2: **Modified Tracked Files** (फाइलें जो पहले से Git में हैं, पर अभी बदली गई हैं)
* Git को पता है ये किस commit से belong करती हैं।
* अगर आप branch बदलते हो और दूसरी branch में उसी file का अलग version है → तो conflict/overwrite हो सकता है।
* इसीलिए Git **allow नहीं करता** जब तक:

  1. आप changes commit न कर दो,
  2. या stash न कर दो,
  3. या discard न कर दो।

-------------------------------- 2. stash ---------------------------------------------
# 1. काम किया (file update की)
echo "update" >> notes.txt

# 2. Changes को stash करो (सिर्फ tracked files)
git stash

# 3. Untracked files भी stash करनी हों
git stash -u

# 4. Untracked + Ignored files भी stash करनी हों
git stash -a

# 5. Custom message के साथ stash करो
git stash push -m "WIP: notes.txt update"

# 6. Stash list देखो
git stash list

# 7. किसी stash का detail देखो (overview)
git stash show stash@{0}

# 8. किसी stash का पूरा diff देखो
git stash show -p stash@{0}

# 9. Latest stash वापस लाओ (stash list में रहेगा)
git stash apply

# 10. Specific stash वापस लाओ
git stash apply stash@{2}

# 11. Latest stash वापस लाओ + stash हटाओ
git stash pop

# 12. Specific stash वापस लाओ + stash हटाओ
git stash pop stash@{1}

# 13. किसी एक stash को हटाओ
git stash drop stash@{0}

# 14. सारे stashes clear कर दो
git stash clear

IMP for stash
Note:- 
1. git stash clear क्या करता है?
ये आपके सारे stashes delete कर देता है।
मतलब जो भी changes आपने stash करके रखे थे, वो अब Git में कहीं save नहीं रहेंगे।
अगर आपने उन्हें पहले apply/pop नहीं किया है → तो वो changes हमेशा के लिए गायब हो जाएँगे।

2. किसी भी branch में जाकर उसे apply या pop कर सकते हो।
कैसे काम करता है Stash?
जब आप git stash चलाते हो, Git आपके current working directory changes को एक special जगह (.git/refs/stash) में save कर देता है।
यह stash किसी branch से permanent जुड़ा नहीं होता।
इसलिए आप किसी भी branch में जाकर उसे apply या pop कर सकते हो।

📒 Git Stash (जब 1 से ज़्यादा stash हों)
# 1. कोई change करो और stash करो
git stash -m "first change"

# 2. दूसरा change करो और फिर stash करो
git stash -m "second change"

# 3. देखो अभी stash list में क्या है
git stash list
# Output:
# stash@{0}: On main: second change
# stash@{1}: On main: first change

# 4. Latest stash (stash@{0}) वापस लाओ और list से हटा दो
git stash pop
# → सिर्फ "second change" वापस आएगा

# 5. फिर stash list देखो
git stash list
# Output:
# stash@{0}: On main: first change

# 6. अब पुराना वाला stash निकालो
git stash pop stash@{0}
# → अब "first change" भी वापस आ जाएगा


================================= 3. Basic ✅ conflict और ❌ no conflict ==============================================
# 📒 Git Merge Notes (With & Without Conflict)
## 🔹 Case 1: Merge **without conflict**

### Flow:
1. उस branch पर जाओ जिसमें merge करना है
   git checkout main

2. Merge करो
   git merge feature

   👉 अगर changes अलग-अलग जगह हैं, तो Git अपने आप merge कर देगा।
   * कभी **fast-forward merge** होगा (pointer सिर्फ आगे बढ़ेगा)।
   * कभी **auto merge commit** होगा (Git खुद commit बना देगा)।

3. Remote पर push करो
   git push origin main

✅ Done (कोई manual commit करने की ज़रूरत नहीं)।

## 🔹 Case 2: Merge **with conflict**
### Flow:
1. उस branch पर जाओ जिसमें merge करना है
   git checkout main

2. Merge करो
   git merge feature

   👉 Git error देगा:
   CONFLICT (content): Merge conflict in notes.txt
   Automatic merge failed; fix conflicts and then commit the result.

3. Conflict वाली files open करो (markers होंगे):
   <<<<<<< HEAD
   main branch ka content
   =======
   feature branch ka content
   >>>>>>> feature

4. File को manually resolve करो और save कर दो।

5. Resolve mark करो

   git add notes.txt

6. Merge commit complete करो
   git commit

7. Remote पर push करो
   git push origin main
✅ Done (अब GitHub पर भी merge दिखेगा)।

# 🔹 Golden Rule (Summary)
| Condition         | Steps                                                                |
| ----------------- | -------------------------------------------------------------------- |
| **No Conflict**   | `checkout` → `merge` → `push`                                        |
| **With Conflict** | `checkout` → `merge` → **resolve files** → `add` → `commit` → `push` |


📒 Git Merge with Multiple Conflicts (Complete Flow)
# 1. उस branch पर जाओ जिसमें merge करना है
git checkout main

# 2. दूसरी branch को merge करने की कोशिश करो
git merge feature

# (अगर multiple files में conflict आया तो Git बताएगा:
# CONFLICT (content): Merge conflict in file1.txt
# CONFLICT (content): Merge conflict in file2.txt
# Automatic merge failed; fix conflicts and then commit the result.)

# 3. हर conflict वाली file खोलो, markers resolve करो और save करो
# <<<<<<< HEAD
# main branch ka code
# =======
# feature branch ka code
# >>>>>>> feature

# 4. Resolve की हुई files को stage करो
git add file1.txt
git add file2.txt
# या shortcut:
git add .

# 5. Merge commit पूरा करो
git commit

# 6. Remote repo (GitHub) पर merge result push करो
git push origin main


============================= 4. Reset ==============================================

## 🔹 Git Reset
`git reset` command से आप अपने **HEAD (current commit pointer)** को पीछे ले जाते हैं।
मतलब: Git history से commit हट सकता है या सिर्फ pointer पीछे जाएगा (mode पर depend करता है)।

Reset के तीन modes हैं:
### 1. **--soft reset**

* HEAD को पीछे ले जाएगा
* Staging area (index) और Working directory **जैसे का तैसा** रहेगा
* मतलब commit undo हो जाएगा, लेकिन changes अभी भी staged रहेंगे

👉 Example:
git reset --soft HEAD~1

यह last commit हटाएगा, लेकिन उसके सारे changes staging area (git add वाले area) में रहेंगे।
➡ Useful जब आपने commit तो कर दिया, लेकिन message या कुछ गलती है, और बस दोबारा commit करना चाहते हो।

### 2. **--mixed reset (default)**
* HEAD पीछे जाएगा
* Staging area खाली हो जाएगा
* Working directory safe रहेगी
* मतलब commit हटेगा और changes unstaged हो जाएँगे

👉 Example:

git reset HEAD~1

यह last commit हटाएगा, और उसके changes वापस working directory (unstaged) में आ जाएँगे।
➡ Useful जब commit भी गलत है और साथ ही staging भी हटानी है।

### 3. **--hard reset**

* HEAD पीछे जाएगा
* Staging area भी साफ़
* Working directory भी साफ़
* मतलब commit + changes दोनों delete हो जाएँगे

git reset --hard HEAD~1

यह last commit भी delete करेगा और उस commit के सारे changes भी मिटा देगा।
➡ Dangerous! क्योंकि data खो सकता है (जब तक reflog से recover न करो)।

| Mode      | Commit हटेगा | Staging area  | Working directory |
| --------- | ------------ | ------------- | ----------------- |
| `--soft`  | हाँ          | Safe (staged) | Safe              |
| `--mixed` | हाँ          | Clear         | Safe              |
| `--hard`  | हाँ          | Clear         | Clear (delete)    |

* **--soft** → Commit हटेगा, changes **staging area + working directory** में रहेंगे।
* **--mixed (default)** → Commit हटेगा, changes **staging से हट जाएंगे, working directory में रहेंगे।**
* **--hard** → Commit हटेगा, changes **staging और working directory दोनों से हट जाएंगे।**


============================= 5. Revert ==============================================

# 📘 Git Revert Notes

* किसी commit के changes को undo करता है।
* Commit **delete नहीं होता**, बल्कि एक **नया commit** बनता है जो उस commit को उल्टा कर देता है।
* History safe रहती है (team work के लिए best)।

## 🔹 Steps
1. **Commit देखें**
   git log --oneline

   Example:
   c3d2f1  add reset concept
   b2a1e9  add paragraph
   a1b2c3  add heading

2. **Commit revert करें**
   git revert c3d2f1

3. **Commit Message** (auto-generate होगा)
   Revert "add reset concept"
   This reverts commit c3d2f1...
   * Save और exit करें
   * या custom message लिखें

4. **History दोबारा देखें**
   git log --oneline

   Result:
   d4e5f6  Revert "add reset concept"
   c3d2f1  add reset concept
   b2a1e9  add paragraph
   a1b2c3  add heading

5. **Remote पर push करें**
   git push origin master

## 🔹 Key Points
* **Reset** commit delete करता है → history बदलेगी ❌
* **Revert** commit को delete नहीं करता → नया commit जोड़ता है ✅
* Safe option है जब आप team में काम कर रहे हों।